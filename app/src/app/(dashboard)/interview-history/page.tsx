import { InterviewPanel } from "@/components/interview/interview-panel";

export default function InterviewHistoryPage() {
  return <InterviewPanel mode="mock" historyFilterMode="all" initialHistoryOpen historyOnly />;
}
